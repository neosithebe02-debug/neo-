# neo-
the website consists of 4 sections: index, about, services, and contact.All sections exist in one index.html file and are controlled using a single page application approach.
JavaScript function showPage() manages navigation by adding/removing the active CSS class to show only the selected page.The URL hash #about #services etc is updated so each page can be bookmarked or refreshed without losing position 
